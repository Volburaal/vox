integer fact(integer n) {
    if (n <= 1) { return 1; }
    return n * fact(n - 1);
}
integer fib(integer n) {
    if (n < 2) { return n; }
    return fib(n - 1) + fib(n - 2);
}
main {
    print(fact(10), '\n');
    print(fib(15), '\n');
}
