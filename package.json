{
  "name": "vox",
  "private": true,
  "workspaces": [
    "core",
    "web"
  ],
  "scripts": {
    "build": "npm run build -w core && npm run build -w web",
    "dev": "npm run build -w core && npm run dev -w web"
  }
}
